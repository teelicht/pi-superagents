# v0.11.1 Dependency Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish `@teelicht/pi-superagents` 0.11.1 with Pi 0.80.7 and all still-applicable open Dependabot updates.

**Architecture:** Keep this a dependency-only patch release. Refresh the existing pnpm lockfile, verify every Pi API used by the extension through typechecking and the complete test suite, and avoid source changes unless verification identifies a real incompatibility.

**Tech Stack:** TypeScript, pnpm, Node.js test runner, Biome, GitHub Releases, npm Trusted Publishing.

## Global Constraints

- Set all direct Pi development dependencies and the Pi host peer dependency to `^0.80.7`.
- Do not downgrade transitive dependencies already newer than open Dependabot PR targets.
- Release version is `0.11.1` and tag is `v0.11.1`.
- Run the complete release gate from `docs/releases.md` before publishing.

---

### Task 1: Refresh dependencies

**Files:**
- Modify: `package.json`
- Modify: `pnpm-lock.yaml`

- [x] Confirm each open Dependabot target is present at that version or newer, or has disappeared from the current graph.
- [x] Update all `@earendil-works/pi-*` development dependencies to `^0.80.7` and the coding-agent peer dependency to `^0.80.7`.
- [x] Regenerate `pnpm-lock.yaml` with pnpm.

### Task 2: Verify Pi compatibility

**Files:**
- Inspect: `src/**/*.ts`
- Test: `test/unit/*.test.ts`
- Test: `test/integration/*.test.ts`
- Test: `test/e2e/*.test.ts`

- [x] Review Pi 0.80.7 release changes against imported extension APIs.
- [x] Run typechecking and all tests; change source only for demonstrated incompatibilities.

### Task 3: Prepare and publish 0.11.1

**Files:**
- Modify: `package.json`
- Modify: `pnpm-lock.yaml`
- Modify: `CHANGELOG.md`
- Modify existing Pi-version references in required user documentation when present.

- [x] Set the package version to `0.11.1` and add dated release notes.
- [x] Run frozen install, typecheck, Biome check, all tests, and npm package dry-run.
- [ ] Commit and push the release commit, tag `v0.11.1`, publish the GitHub Release, and verify its workflow.
